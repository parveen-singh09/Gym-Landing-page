import type { Metadata } from "next";
import { Plus_Jakarta_Sans } from "next/font/google";
import "./globals.css";

const plusJakartaSans = Plus_Jakarta_Sans({
  variable: "--font-jakarta",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "TITAN | Premium Fitness Gym & Performance Center",
  description: "Experience training at TITAN Gym. Be healthier, be stronger, and be confident with our expert trainers, world-class athletic facilities, and tailored fitness programs.",
  keywords: ["gym", "fitness", "personal training", "TITAN", "athletic conditioning", "strength training"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${plusJakartaSans.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col font-sans">{children}</body>
    </html>
  );
}

